export const baseApiConfig = {
  base: "/api/v1",
};

export const healthApiConfig = {
  getHealth: "/health",
};

export const authRouteConfig = {
  signup: "/signup",
  login: "/login",
  logout: "/logout",
};
